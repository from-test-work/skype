mixin platform(data)
    +b.platform
        +e('ul').list
            each item in data.items
                +e('li').item
                    +e('a').link(href="#")
                        +e('img').img(src= item.src alt= item.alt)
        +e.note= data.note
